# Vivora — runtime dependencies
# Tested on Python 3.11+ (Streamlit Community Cloud uses Python 3.11).

# Use PyTorch's CPU-only wheels first, then fall back to PyPI for the rest.
# This keeps torch ~200 MB instead of ~2 GB and fits inside Streamlit Cloud's
# 1 GB app size budget.
--extra-index-url https://download.pytorch.org/whl/cpu

torch
torchvision

# UI
streamlit

# LLM + embeddings + RAG
langchain
langchain-core
langchain-text-splitters
langchain-chroma
langchain-huggingface
langchain-groq
chromadb
sentence-transformers

# chromadb pulls in opentelemetry-proto whose generated _pb2.py files use the
# pre-5.x protobuf descriptor API. Without this pin pip resolves protobuf 5+,
# which removed that API and breaks chromadb's import chain.
protobuf<5

# Repo cloning
gitpython

# Output generation
fpdf2          # viva-questions PDF
python-docx    # project-report Word
python-pptx    # slides PowerPoint

# Config
python-dotenv
