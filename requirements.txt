# Vivora — runtime dependencies
# Tested on Python 3.11+ (Streamlit Community Cloud also runs Python 3.x).

# Use PyTorch's CPU-only wheels first, then fall back to PyPI for the rest.
# Keeps torch ~200 MB instead of ~2 GB so we fit inside Streamlit Cloud's
# 1 GB app budget.
--extra-index-url https://download.pytorch.org/whl/cpu

torch
torchvision

# UI
streamlit

# LLM + embeddings + RAG
langchain
langchain-core
langchain-community     # provides langchain_community.vectorstores.FAISS
langchain-text-splitters
langchain-huggingface
langchain-groq
faiss-cpu               # lightweight vector store, no SQLite/telemetry layer

# transformers 5.x eagerly imports hundreds of image-processor modules at
# startup, blowing Streamlit Cloud's 1 GB memory cap. v4.x is far lighter and
# fully compatible with sentence-transformers / langchain-huggingface.
sentence-transformers<3
transformers>=4.40,<5

# Repo cloning
gitpython

# Output generation
fpdf2          # viva-questions PDF
python-docx    # project-report Word
python-pptx    # slides PowerPoint

# Config
python-dotenv
