# Vivora — runtime dependencies
# Tested on Python 3.11+ (Streamlit Community Cloud runs Python 3.x).

# UI
streamlit>=1.57

# LLM
langchain
langchain-core
langchain-community     # vector store + fastembed wrapper
langchain-text-splitters
langchain-groq

# Embeddings: fastembed uses ONNX runtime (~80 MB total) instead of
# the torch + transformers + sentence-transformers stack (~700 MB)
# that was OOM-killing the Streamlit Cloud worker on its 1 GB cap.
fastembed

# Vector store
faiss-cpu

# Repo cloning
gitpython

# Output generation
fpdf2          # viva-questions PDF
python-docx    # project-report Word
python-pptx    # slides PowerPoint

# Config
python-dotenv
